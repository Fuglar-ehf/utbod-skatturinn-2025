# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build skatturinn-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t skatturinn-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system skatturinn-api && \
          adduser --system -G skatturinn-api skatturinn-api

COPY apps/skatturinn-api/dist skatturinn-api/
RUN chown -R skatturinn-api:skatturinn-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix skatturinn-api --omit=dev -f install

CMD [ "node", "skatturinn-api" ]
